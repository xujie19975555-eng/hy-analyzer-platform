# HY Analyzer Platform - Environment Variables

# Claude API (via relay or direct)
ANTHROPIC_API_KEY=cr_xxx
ANTHROPIC_BASE_URL=https://co.yes.vg
# ANTHROPIC_BASE_URL=https://api.anthropic.com  # Official API

# OpenAI API (optional, if not using dual Claude mode)
# OPENAI_API_KEY=sk-xxx
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o

# Dual Claude mode: Use Claude Sonnet + Haiku instead of Claude + OpenAI
USE_DUAL_CLAUDE=true

# AI Scoring Settings
AI_SCORING_ENABLED=true
AI_SCORING_TIMEOUT=60.0

# Cache Settings
CACHE_VALID_HOURS=1.0  # Analysis cache validity (hours), set to 0 to disable

# Database (optional, defaults to SQLite)
# DATABASE_URL=sqlite:///./traders.db
